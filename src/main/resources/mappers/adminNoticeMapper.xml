<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.persistence.AdminNoticeDAO">

	<!-- 관리자 공지사항 글쓰기 -->
	<insert id="insertNotice" parameterType="com.itwillbs.domain.NoticeVO">
	  INSERT INTO notice (ad_id, notice_title, notice_content, fixed, notice_date)
	  VALUES (#{ad_id}, #{notice_title}, #{notice_content}, #{fixed}, NOW())
	</insert>

	<!-- 공지사항 상세/수정/삭제 -->
	<select id="getNoticeById" resultType="com.itwillbs.domain.NoticeVO">
	  SELECT * FROM notice WHERE notice_id = #{notice_id}
	</select>
	
	<update id="updateNotice">
	  UPDATE notice
	  SET notice_title = #{notice_title},
	      notice_content = #{notice_content},
	      fixed          = #{fixed}
	  WHERE notice_id = #{notice_id}
	</update>
	
	<delete id="deleteNotice">
	  DELETE FROM notice WHERE notice_id = #{notice_id}
	</delete>
	
	<!-- 페이징된 공지사항 목록 조회 -->
	<select id="getNoticeListPage" resultType="com.itwillbs.domain.NoticeVO">
	  SELECT notice_id, notice_title, notice_date, fixed
      FROM notice
      ORDER BY fixed DESC, notice_id DESC
      LIMIT #{startRow}, #{pageSize}
	</select>
	
	<!-- 전체 글 개수 조회 -->
	<select id="getNoticeCount" resultType="int">
	  SELECT COUNT(*) FROM notice
	</select>
	
	
	
	
	
	
	
	
	

</mapper>  
  
  